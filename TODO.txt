1. [RESOLVED] Piped commands without shell will fail - Commands are executed through shell via temp files, which handles pipes correctly
2. [RESOLVED] Switching users can work only under sudo in nix OS - Added warning when attempting user switch without root privileges (UID check)
