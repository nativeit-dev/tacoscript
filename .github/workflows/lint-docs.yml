name: Lint docs
on: [push]
jobs:
  docs:
    name: Lint docs
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Markdown Lint
        run: |
          cd docs
          npx markdownlint-cli2 content/**/*.md