name: Build and deploy docs
on:
  push:
    branches:
      - master
      - docs
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  docs:
    name: Build and deploy docs
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Markdown Lint
        run: |
          cd docs
          npx markdownlint-cli  content/

      - name: Build with Hugo
        run: |
          pwd
          curl -sL https://github.com/gohugoio/hugo/releases/download/v0.139.4/hugo_0.139.4_linux-amd64.tar.gz|tar xzf - hugo
          mv hugo ~/
          ~/hugo --version||true
          cd docs
          test -e public||mkdir public
          ~/hugo --minify
          find public

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './docs/public'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4