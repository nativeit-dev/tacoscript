name: Test Linux
on: [push]
jobs:
  test:
    name: Test Linux
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Test Linux
        run: go test -race ./...